/**
 * Deprecated Supabase client stub.
 * The Carol Ann Cloud OS backend has been fully migrated to Firebase Firestore & Firebase Auth.
 */
export const isSupabaseConfigured = false;
export const isMigratedToFirebase = true;
